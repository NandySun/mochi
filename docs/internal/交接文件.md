# Mochi — 交接文件

> 2026-06-21 · 设计语言深化完成

---

## 项目概览

桌面原生个人多媒体库。Tauri v2 + React 19 + TypeScript + Tailwind + SQLite + libmpv + framer-motion 11。
GitHub 公开仓库：`https://github.com/NandySun/mochi`

---

## 当前架构

| 路由 | 组件 | 职责 |
|---|---|---|
| `/` | PosterWall | 海报横滚（响应式 vw/clamp）+ 筛选栏 + 标题 + 焦点圆点 |
| `/series/:id` | SeriesDetail | 系列信息 + 剧集列表（纯浏览） |
| `/play/:episodeId` | VideoPlayer | 纯播放：顶栏 + 视频区 + OSC（含选集📋）+ 全屏 auto-hide |
| 弹窗 | Settings | 媒体库/元数据/OSC 主题/关于，portal 注入 body |

**全局背景**：`GlobalBackground`（App 级三层 fixed 浮层）通过 `useBackground().setBg()` 控制。浏览页完整氛围背景，播放页 gradient 透明 + fanart null，视频透底。

**全屏**：Tauri 原生 `set_fullscreen` + 统一布局条件样式。OSC/顶栏/光标 3s 无操作自动淡出。

**OSC 主题**：5 种可切换预设（Mochi/YouTube/PotPlayer/Netflix/极简），`src/themes/oscThemes.ts`，设置页选择器。

---

## 文件清单

| 文件 | 说明 |
|---|---|
| `src/App.tsx` | 路由 + TitleBar + GlobalBackground + 全局快捷键 + 设置弹窗 |
| `src/components/PosterWall.tsx` | 首页：海报横滚 + 筛选栏 + 标题 + 焦点圆点（JS 软 snap） |
| `src/components/SeriesDetail.tsx` | 详情页：海报 + 信息 + 剧集网格（stagger 入场） |
| `src/components/VideoPlayer.tsx` | 播放页：顶栏 + 视频区 + OSC + 自动连播 + 进度保存 |
| `src/components/OscBar.tsx` | OSC 控制条（framemotion 声明式交互动效） |
| `src/components/Settings.tsx` | 设置弹窗（媒体库/元数据/OSC 主题/关于） |
| `src/components/TitleBar.tsx` | 自定义标题栏 + 窗口控制 |
| `src/components/PageWrapper.tsx` | 统一页面进出动画容器，方向感知 |
| `src/components/BreathingDot.tsx` | 加载态呼吸圆组件（16/24/48px）+ ScanBreathing |
| `src/components/WindowResizeHandles.tsx` | 窗口缩放拖拽手柄（transparent 窗口 resize border 替代） |
| `src/animations/tokens.ts` | 集中 spring 参数 + pageAnimations 预设 |
| `src/hooks/useBackground.tsx` | 全局背景 Context + GRADIENTS + 颜色温度叙事 |
| `src/hooks/useMpv.ts` | mpv 单例封装（含 resume seek、auto-stop） |
| `src/hooks/useFullscreen.ts` | Tauri 原生全屏 |
| `src/hooks/useImageSrc.ts` | 图片 base64 缓存 |
| `src/hooks/useNavigationDirection.tsx` | 导航方向 Context |
| `src/themes/oscThemes.ts` | OSC 主题类型 + 5 个预设 |
| `src/utils/color.ts` | 颜色温度工具：gradient → HSL → 冷暖判断 |

---

## 已知约束

**不可动：**
- mpv 单例，不销毁重建
- `refreshTracks` 不可用：`track-list` node 序列化在 libmpv-wrapper v0.1.1 + libmpv-2 v0.41.0 下崩溃
- 关闭窗口 = hide 到托盘（`CloseRequested` 拦截），托盘右键退出
- resume seek 必须在 observer 收到 `duration` 回调后执行（文件完全加载前 seek 被忽略）

**v0.1.1 中修过的关键 Bug（防止回退）：**
- 离开播放页 → `handleBack` 必须 await 保存进度 + `cleanup` 加 `command("stop")`
- 自动连播不能走 `saveAndGo`（>95% progress 会误标 `watched_completed`）
- `WebView2Loader.dll` 必须和 exe 一起打包到便携版

---

## 编译运行

```powershell
cd D:\Work\OH-WorkSpace\projects\mochi
npx tauri dev    # 开发
npx tauri build  # 生产 → src-tauri/target/release/mochi.exe
```

## 内部文档索引

| 文档 | 用途 |
|---|---|
| `设计草案.md` | 核心愿景、设计原则、UI 架构、数据模型 |
| `设计语言深化方案.md` | 材质物理三定律 + 六项设计深化（2026-06-21） |
| `设计语言深化实施记录.md` | 六项实施详情 + 踩坑记录 |
| `交互优化记录.md` | v0.1.2~v0.1.3 动效工程历史记录 |

## 新对话快速启动

```
接手 Mochi 开发维护。
项目路径 D:\Work\OH-WorkSpace\projects\mochi。
技术栈：Tauri v2 + React 19 + TS + Tailwind + SQLite + libmpv + framer-motion 11。

先读本文件 → 设计草案.md。
设计语言相关查阅 设计语言深化方案.md + 实施记录。
docs/internal/ 是内部文档（.gitignore 保护），可随时查阅。
```
